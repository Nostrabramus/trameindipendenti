lib.header {
    image = IMAGE
    image {
        file {
            import {
                filelist = fileadmin/immagini/header-homepage | jpg,jpeg,png,gif | name | | 1
                listNum = rand
            }
        }
        params = class="home-banner"
    }
}